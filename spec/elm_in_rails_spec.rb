require "spec_helper"

describe ElmInRails do
  it "has a version number" do
    expect(ElmInRails::VERSION).not_to be nil
  end

  it "does something useful(like true = true)" do
    expect(true).to eq(true)
  end
end
